<!DOCTYPE html>
<html>
{{> head.hbs}}
<body>
  <main>
    <img src="logo.png" alt="Brocessing">
    <h1>{{title}}</h1>
    <p>{{nl2br message}}</p>
    <ul>
      {{#each contact}}
        <li><a href="{{url}}">{{name}}</a></li>
      {{/each}}
    </ul>
  </main>
  <script src="{{compiler.hash}}.js"></script>
</body>
</html>